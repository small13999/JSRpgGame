export class Item {
    constructor(text, x, y, width, height, itemObject) {
        this.text = text;
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
        this.itemObject = itemObject;
    }
}